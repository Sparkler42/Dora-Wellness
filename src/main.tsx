import React from 'react'
import ReactDOM from 'react-dom/client'
import SparkApp from './SparkApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SparkApp />
  </React.StrictMode>,
)
